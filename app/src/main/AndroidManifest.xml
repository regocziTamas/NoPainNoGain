<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codecool.nopainnogain">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WorkoutDetails" />
        <activity
            android:name=".PlayWorkout"
            android:label="@string/title_activity_play_workout" />
        <activity android:name=".ExerciseDetails" />
        <!-- <activity android:name=".SplashScreen"></activity> -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CreateNewWorkout"
            android:label="Create New Workout" />
        <activity android:name=".EditBlock" />
        <activity android:name=".AddRest" />
        <activity android:name=".AddExercise" />
        <activity android:name=".SelectNewExercise"></activity>
    </application>

</manifest>